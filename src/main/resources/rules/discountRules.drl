package rules;

import net.bounceme.chronos.chguadalquivir.model.*;

dialect  "mvel"

rule "Ajuste de precio de producto"
	when
    	$p : ProductPrice(basePrice > 2 )
    then
    	System.out.println("EJECUTANDO -Ajuste de precio de producto- para el producto [" + $p + "]");
end