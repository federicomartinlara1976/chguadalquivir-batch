spring:
  application:
    name: CHGuadalquivir
    description: Recover data from C. H. Guadalquivir

  main.allow-bean-definition-overriding: true
  
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  
  datasource:
    batch:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url:               jdbc:mysql://localhost/batch?allowPublicKeyRetrieval=true&useSSL=false
      username:          federico
      password:          MedinaAzahara2468@
    
  batch.job.enabled: false
  
  data.mongodb.uri: mongodb://localhost:27017/EmbalsesCHG
  
#  jpa:
#    show-sql: true
#    properties.hibernate.format_sql: true
  
server:
  port : 9091
  servlet.context-path: /chguadalquivir
  
batch:
  jdbc:
    testWhileIdle: true
    validationQuery: SELECT 1
    batch.jdbc.pool.size: 6
  
  database:
    incrementer:
      class: org.springframework.jdbc.support.incrementer.MySQLMaxValueIncrementer
      parent: columnIncrementerParent

  lob.handler.class: org.springframework.jdbc.support.lob.DefaultLobHandler
  grid.size: 50
  verify.cursor.position: true
  initialize-schema: always

c3p0:
  user: federico
  password: MedinaAzahara2468
  driverClass: org.postgresql.Driver
  jdbcUrl: jdbc:postgresql://localhost/prueba
  minPoolSize: 1
  maxPoolSize: 1
  maxIdleTime: 500

application:
  importJob:
    cron: 0 45 9 ? * 1,2,3,4,5  
    url: https://www.chguadalquivir.es/saih/EmbalNiv.aspx
   
  lastExecutions:
    cron: 0 0 10 ? * 5
    export:
      file:
        header: ID,VALUE,EXECUTIONTIME
        path: /home/federico/logs/lastExecutions
        suffix: csv
        
  statExecutions:
    cron: 0 30 10 ? * 5
    